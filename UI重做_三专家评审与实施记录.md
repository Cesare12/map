# 地图 UI 重做：三专家独立评审与实施记录

日期：2026-09-13

## 独立评审结论

### 产品与交互

- 地图上方和下方的常驻信息过多，选中卡与附近面板同时出现会遮挡地图。
- 用户保存了地点笔记，但地图选中卡没有显示，无法帮助用户回忆种草原因。
- 分类负责“看什么”，状态负责“进行到哪”，距离负责“去哪”，三者需要分层表达。
- 新增保存后应让地点立即可见并高亮；主动点击当前位置时应刷新定位。

### 视觉设计

- 大标题、渐变蒙层、多组计数、双 Emoji 标签和多层阴影让界面显得拥挤。
- 采用白色轻面板、深绿色单一强调色、统一间距和一个底部容器。
- 地图使用有明确落点的统一图钉；选中地点时用底部卡片承载信息，不再重复显示地图气泡。
- 编辑页去掉大面积黑色头图和黄色主按钮，与地图页统一为绿色视觉体系。

### 微信小程序工程

- 原 `TRANSPARENT_PNG` 解码后是灰度 0、Alpha 255 的不透明黑色像素，并被 marker 放大为 30×40，确定是新增地点后黑框的根因。
- 只修改 Base64 仍会受旧沙盒文件缓存影响，因此应彻底改为代码包内的真实图钉资源。
- marker ID 不应由筛选后的数组序号生成，否则切换筛选后地图可能复用错误状态；应为地点分配稳定 ID。
- 地图 `label` 和 `callout` 是原生字段，复杂内容应放到 WXML 底部卡片。

## 综合后采用的方案

1. 删除运行时 1×1 占位图、地图双 Emoji label 和 callout。
2. 使用 8 个包内 PNG 图钉：美食、宠物、洗车、其他各有种草与已拔草版本。
3. 顶部压缩为一行标题与状态分段，加一行横向分类。
4. 附近地点与选中地点卡片互斥显示，避免叠卡。
5. 选中卡展示地点笔记、距离、到访次数、导航和状态操作。
6. 新增或编辑保存后自动回到并高亮该地点。
7. 点击“我的位置”重新获取定位并重新计算距离。
8. 使用稳定的地点到 marker 数字 ID 映射。

## 未纳入本轮

- 地点聚合：需要结合真实数据密度和真机性能单独验证。
- 照片与逐次到访感想：涉及数据结构、文件容量和备份规则，继续按后续版本推进。
- 自定义底图样式：当前先优化可控的图钉、卡片和信息层级。
